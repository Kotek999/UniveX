import React, { FC } from "react";
import { Text } from "react-native";

const Log: FC = () => {
  return (
    <Text style={{ marginTop: 20, textAlign: "center" }}>
      Zalogowany jesteś
    </Text>
  );
};

export default Log;
